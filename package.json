{
  "name": "@types/cookie-session",
  "version": "2.0.0-alpha.1",
  "description": "Types for https://github.com/expressjs/cookie-session",
  "scripts": {
    "test": "npm run lint && npm run build && npm run test-spec",
    "test-spec": "ts-node test/index.ts",
    "build": "typings bundle . --out bundle.d.ts",
    "lint": "tslint index.d.ts",
    "prepublish": "typings install"
  },
  "devDependencies": {
    "cookie-session": "^2.0.0-alpha.1",
    "ts-node": "^1.3.0",
    "tslint": "^3.7.4",
    "tslint-config-typings": "^0.2.3",
    "typescript": "^1.8.9",
    "typings": "^1.3.3"
  }
}
